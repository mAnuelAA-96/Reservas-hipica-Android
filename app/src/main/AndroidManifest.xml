<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Reservas_hipica"
        tools:targetApi="31">
        <activity
            android:name=".ui.RegisterActivity"
            android:theme="@style/LoginRegister.Theme.reservas_hipica"
            android:label="Registrar usuario"
            android:exported="false" />
        <activity
            android:name=".ui.EditReserva"
            android:theme="@style/Edit.Theme.reservas_hipica"
            android:label="Editar reserva"
            android:exported="false" />
        <activity
            android:name=".ui.AddReserva"
            android:theme="@style/Add.Theme.reservas_hipica"
            android:label="Nueva reserva"
            android:exported="false" />
        <activity
            android:name=".ui.MisReservas"
            android:theme="@style/MisReservas.Theme.reservas_hipica"
            android:label="Mis reservas"
            android:exported="false" />
        <activity
            android:name=".ui.MainActivity"
            android:theme="@style/LoginRegister.Theme.reservas_hipica"
            android:label="Caballos para disfrutar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>